#  Simple Calculator

## Overview
A modern, web-based **Calculator** built with **HTML, CSS, and JavaScript**.  
This project demonstrates core frontend development skills including DOM manipulation, event handling, and responsive UI design.

##  Features
- Perform basic arithmetic operations: **Addition (+), Subtraction (−), Multiplication (×), Division (÷)**
- **Clear (C) button** to reset the display
- **Decimal support** for floating-point calculations
- **Equals (=) button** to compute results
- Clean, centered layout with gradient background for visual appeal
- Responsive design for different screen sizes

##  Project Structure
```
FrontEnd-Projects/
│
└── calculator/
    ├── index.html    # Structure of the calculator
    ├── style.css     # Styling and layout
    └── script.js     # Logic and interactivity
```
##  Learning Highlights
- Practiced **semantic HTML** for structured markup
- Applied **CSS styling** for modern UI with gradient backgrounds and color-coded buttons
- Implemented **JavaScript event listeners** to handle user input and perform calculations
- Strengthened skills in **DOM manipulation** and **responsive design**

